# HH Backend (FastAPI)

## Требования
- Python 3.13+
- (Dev) SQLite (по умолчанию), (Prod) PostgreSQL
- Redis

## Установка
```bash
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
