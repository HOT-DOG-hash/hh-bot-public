# --- Python venv / cache / builds
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# --- IDE / OS junk
.idea/
.vscode/
.DS_Store
Thumbs.db
._*
*.log
desktop.ini

# --- Node / PHP
node_modules/
front_bot_php/vendor/
*.phar            # composer.phar и др. бинарники PHP

# --- Databases
*.sqlite
*.db

# --- ENV & keys (keep example!)
.env
.env.prod
.env.bak
*.env
!.env.example     # <-- шаблон значений храним в репо
HH бот/.env.dev
nginx/htpasswd
*.pem
*.key
*.p12
*.keystore
*.crt
*.cert

# --- Local configs that must not be committed
config.local.*.json
secrets*.json
secrets*.yml
front_bot/.env
front_bot/config_secrets.py

# Если это реальный код — лучше его не игнорировать,
# а вынести секреты в ENV. Пока оставляем:
backend/app/core/local_settings.py
backend/app/core/config_prod.py

# --- Test/Lint/Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# --- Secret scanners & mirrors (артефакты)
tools/reports/
.mirror.git/
.remote-mirror.git/
gitleaks-history.json
trufflehog-*.json
trufflehog-files.json

# --- Project-specific runtime/logs
logs/
.diagnostics/

# --- Docker local overrides (если не хочешь держать их в репо)
docker-compose.override.yml
