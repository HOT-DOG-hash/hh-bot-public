# P1.2 — Observability & Alerts (2025-11-04T14:55Z)

## Summary
- Realigned `docs/ops/observability_spec.md` with actual FastAPI readiness checks and exported Prometheus metrics (`campaign_*`, `delivery_*`, `payments_*`, `webhook_*`).
- Added live metric samples under `checklist-progress/artifacts/P1.2/metrics_examples.txt` to back the contract.
- Produced alerting skeleton `ops/alerts/p1.rules.example.yaml` consistent with the agreed SLOs.

## Follow-up
- Wire the new rule group into production Prometheus configuration/Helm charts.
- Ensure blackbox job `hh-bot-readyz` is configured so `probe_success{job="hh-bot-readyz"}` is emitted for the readiness alert.
