*          text=auto

# Скрипты и env всегда LF
*.sh       text eol=lf
*.env      text eol=lf
Dockerfile text eol=lf

# Конкретные файлы в каталоге "HH бот" — пробел экранируем слэшем
HH\ бот/bot/entrypoint.sh text eol=lf
HH\ бот/bot/runner.py     text eol=lf
